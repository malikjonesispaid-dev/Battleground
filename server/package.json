{
  "name": "jarvis-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal proxy that holds the Anthropic API key server-side so the Jarvis app doesn't need one from each user.",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
