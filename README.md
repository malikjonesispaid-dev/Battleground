# J.A.R.V.I.S. for Android — moved

This app now lives at **[malikjonesispaid-dev/Jarvis](https://github.com/malikjonesispaid-dev/Jarvis)**.

Battleground's `main` branch was later repurposed for an unrelated project, which
collided with these files, so the canonical copy — including everything built here
plus a backend proxy that lets people skip bringing their own Anthropic API key —
was moved to its own repo. Go there for the source, the downloadable APK (via
GitHub Actions), and further development.

This branch is kept only so the history isn't lost; don't build new Jarvis features
here.
